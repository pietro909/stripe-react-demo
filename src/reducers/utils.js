export const makeError = (message) => ({
  level: 3,
  message: message
})

export const makeWarning = (message) => ({
  level: 2,
  message: message
})

export const makeInfo = (message) => ({
  level: 1,
  message: message
})

